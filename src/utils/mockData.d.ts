import { Review, ChatMessage } from '../services/businessService';

export const mockReviews: Review[];
export const mockChats: Map<string, ChatMessage[]>;
export function generateMoreReviews(): Review[];
